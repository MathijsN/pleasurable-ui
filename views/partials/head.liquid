<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>

    <link rel="icon" href="/assets/snappthis-logo.png" type="image/png">

    <link rel="stylesheet" href="/styles.css">
    <script src="/script.js" type="module"></script> 
    <script src="/login.js" type="module"></script>
</head>
<body>
    {% comment %} voor snapp.liquid {% endcomment %}
    {% if oneSnapp %}
        {%  render 'partials/snapp-actions.liquid', 
            snappUuid: snappUuid,
            oneSnappInfo: oneSnappInfo,
            likesCount: likesCount,
            tomatoCount: tomatoCount,
            starCount: starCount,
            status: status,
            hasLike: hasLike,
            hasTomato: hasTomato,
            hasStar: hasStar %}
        {% endif %}
        {% render 'partials/top-nav.liquid', currentPage: currentPage, snappmap: snappmap %}
    <main>
